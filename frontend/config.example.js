// Copy this file to `config.js` and fill in your values.
// `config.js` is gitignored so your real project URL stays out of the repo.
//
// NOTE: The Supabase ANON key is designed to be public — it is safe in
// frontend code AS LONG AS Row Level Security is enabled on every table
// (see supabase/schema.sql). Never put the SERVICE ROLE key here.

window.VOID_CONFIG = {
  SUPABASE_URL: "https://your-project-ref.supabase.co",
  SUPABASE_ANON_KEY: "your-anon-public-key-here",
  API_BASE_URL: "http://localhost:5000",
};
